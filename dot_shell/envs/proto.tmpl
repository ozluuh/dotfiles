{{ template "file_title" }}
{{ template "file_location" . }}
if ! command_exists "proto" && is_zero_size "${HOME}/.proto"; then
  return
fi
export PROTO_HOME="${HOME}/.proto"
export PATH="${PROTO_HOME}/shims:${PROTO_HOME}/bin:${PATH}"
