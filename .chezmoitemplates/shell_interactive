{{ template "file_title" "File loaded on interactive shell sessions" }}
{{ template "file_location" . }}
# If not running interactively, don't do anything
[[ $- != *i* ]] && return
source "${HOME}/.shell/shellrc" 2>/dev/null || echo "Bare 'shell' not loaded"
